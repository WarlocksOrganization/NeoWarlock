using UnityEngine;
using System.Collections.Generic;
using UI;

public class QuickSlotDescription : MonoBehaviour
{
    //public List<QuickSlot> quickSlots; // UI 내 QuickSlot 리스트

    //public void AssignSkillInfoToSlot(int slotIndex, string skillName, string skillDescription)
    //{
    //    if (slotIndex < 0 || slotIndex >= quickSlots.Count)
    //    {
    //        Debug.LogWarning("잘못된 슬롯 인덱스!");
    //        return;
    //    }

    //    quickSlots[slotIndex].SetQuickSlotInfo(skillName, skillDescription);
    //    Debug.Log($"{skillName}");
    //}
}
